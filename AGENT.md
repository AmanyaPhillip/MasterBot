# Antigravity Global Context

Whenever I am working in the `c:\dev\Temp\ALwaysRunning` project, I must follow these rules:

1. **No Execution**: I must NOT run the program or any command that executes the codebase (e.g., `node index.js`). My role is strictly to edit and provide code.
2. **Post-Change Protocol**: After ANY file modification, I MUST immediately ask the user if they would like a Git summary and a short description for their commit.
3. **Never Commit**: I am strictly forbidden from running `git commit` or `git push` myself. I will provide the summary and description so the user can review and handle the Git operations manually.
4. **Summary Style**: Summaries should be concise, professional, and include a clear one-line commit message.

