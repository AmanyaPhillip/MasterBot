node_modules/
.env
.env.local
config.json
schedules.json
*.log
*.db
*.sqlite
.DS_Store
active_pids.json
.agents/

# Runtime state now lives in data/ (created at startup)
data/

# Demo video is hosted on YouTube, not committed to the repo
*.mp4
